<!-- Loop over each object in the projects array -->
{{#each tasks as |task| }}
<div class="column is-4 project">
  <div class="card">
    <header class="card-header {{task.color}} {{#unless task.is_completed}}
  color-problem
  {{/unless}}">
      <p class="card-header-title">
        <a class="light" href="/task/{{task.id}}">{{task.category}}</a>
      </p>
    </header>
    <div class="card-content">
      <div class="content">
        <p>
          <span for="img" aria-label="calendar">📅</span>
          <!-- Pass needed_funding value to the helper function -->
          <span class="time-needed">{{task.hours}}</span>
          hours needed!
        </p>
        <p>
          {{task.category_sub}}
        </p>
        <p>Day {{ task.day}}</p>
      </div>
    </div>
  </div>
</div>
{{/each}}
